fun factorial(n) {
	if n < 1 then {
		return 1
	} else {
		return n * factorial(n - 1) // Call context here, should inherit ?
	}
}

fun main() { // Main has some context ?
	output factorial(2); // Gets call context 1 {n := 2}
}
